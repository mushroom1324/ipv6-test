name: Test ipv6

on:
  push:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  ssh:
    name: Connection Test
    runs-on: ubuntu-latest

    steps:
      - name: test ipv4
        uses: appleboy/ssh-action@master
        with:
          host: 52.78.103.215
          username: ubuntu
          key: ${{ secrets.KEY }}
          port: 22
          script: |
            whoami
            
      - name: test ipv6
        uses: appleboy/ssh-action@master
        with:
          host: 2406:da12:1e4:2596:67d4:870c:c78:c502
          username: ubuntu
          key: ${{ secrets.KEY }}
          protocol: tcp6
          port: 2200
          script: |
            whoami
